<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.kh.project.member.mapper.MemberMapper">

    <!-- ResultMap: MEMBER 테이블 -> MemberDTO 매핑 -->
    <resultMap id="memberResultMap" type="edu.kh.project.member.dto.MemberDTO">
        <id property="memberNo" column="MEMBER_NO" />
        <result property="memberEmail" column="EMAIL" />
        <result property="memberPw" column="PASSWORD" />
        <result property="memberNickname" column="NICKNAME" />
        <result property="memberName" column="NAME" />
        <result property="memberPhone" column="PHONE" />
        <result property="memberGender" column="GENDER" />
        <result property="memberAgeGroup" column="AGE_RANGE" />
        <result property="memberProfileImg" column="PROFILE_IMAGE" />
        <result property="memberIntro" column="INTRO" />
        <result property="memberRole" column="MEMBER_TYPE" />
        <result property="memberStatus" column="STATUS" />
        <result property="emailVerified" column="EMAIL_VERIFIED" />
        <result property="verifiedReviewer" column="VERIFIED_REVIEWER" />
        <result property="loginType" column="LOGIN_TYPE" />
        <result property="createdAt" column="CREATED_AT" />
        <result property="updatedAt" column="UPDATED_AT" />
        <result property="postCount" column="POST_COUNT" />
        <result property="reviewCount" column="REVIEW_COUNT" />
        <result property="companionCount" column="COMPANION_COUNT" />
        <result property="likeCount" column="LIKE_COUNT" />
        <result property="accommReviewCount" column="ACCOMM_REVIEW_COUNT" />
    </resultMap>

    <!-- 이메일로 회원 조회 (로그인용 - 비밀번호 포함) -->
    <select id="selectMemberByEmail" resultMap="memberResultMap">
        SELECT
            MEMBER_NO,
            EMAIL,
            PASSWORD,
            NICKNAME,
            NAME,
            PHONE,
            INTRO,
            PROFILE_IMAGE,
            MEMBER_TYPE,
            STATUS,
            EMAIL_VERIFIED,
            VERIFIED_REVIEWER
        FROM MEMBER
        WHERE EMAIL = #{memberEmail}
          AND STATUS = 'A'
    </select>

    <!-- 회원 번호로 회원 조회 (경량 — 통계 미포함) -->
    <select id="selectMemberByNo" resultMap="memberResultMap">
        SELECT
            M.MEMBER_NO, M.EMAIL, M.PASSWORD, M.NICKNAME, M.NAME, M.PHONE,
            M.GENDER, M.AGE_RANGE, M.PROFILE_IMAGE, M.INTRO,
            M.MEMBER_TYPE, M.STATUS, M.EMAIL_VERIFIED, M.VERIFIED_REVIEWER, M.CREATED_AT
        FROM MEMBER M
        WHERE M.MEMBER_NO = #{memberNo}
    </select>

    <!-- 회원 번호로 회원 조회 (통계 포함 — 프로필 페이지용, LEFT JOIN 집계) -->
    <select id="selectMemberWithStats" resultMap="memberResultMap">
        SELECT
            M.MEMBER_NO, M.EMAIL, M.NICKNAME, M.NAME, M.PHONE,
            M.GENDER, M.AGE_RANGE, M.PROFILE_IMAGE, M.INTRO,
            M.MEMBER_TYPE, M.STATUS, M.EMAIL_VERIFIED, M.VERIFIED_REVIEWER, M.CREATED_AT,
            NVL(pc.CNT, 0)  AS POST_COUNT,
            NVL(rc.CNT, 0)  AS REVIEW_COUNT,
            NVL(jc.CNT, 0)  AS COMPANION_COUNT,
            NVL(lc.CNT, 0)  AS LIKE_COUNT,
            NVL(ac.CNT, 0)  AS ACCOMM_REVIEW_COUNT
        FROM MEMBER M
        LEFT JOIN (SELECT MEMBER_NO, COUNT(*) AS CNT FROM BOARD WHERE STATUS = 'A' GROUP BY MEMBER_NO) pc
            ON pc.MEMBER_NO = M.MEMBER_NO
        LEFT JOIN (SELECT MEMBER_NO, COUNT(*) AS CNT FROM COMPANION_REVIEW WHERE STATUS = 'Y' GROUP BY MEMBER_NO) rc
            ON rc.MEMBER_NO = M.MEMBER_NO
        LEFT JOIN (SELECT MEMBER_NO, COUNT(*) AS CNT FROM COMPANION_JOIN WHERE STATUS IN ('W', 'A') GROUP BY MEMBER_NO) jc
            ON jc.MEMBER_NO = M.MEMBER_NO
        LEFT JOIN (SELECT MEMBER_NO, COUNT(*) AS CNT FROM BOARD_LIKE GROUP BY MEMBER_NO) lc
            ON lc.MEMBER_NO = M.MEMBER_NO
        LEFT JOIN (SELECT MEMBER_NO, COUNT(*) AS CNT FROM ACCOMMODATION_REVIEW WHERE STATUS = 'A' GROUP BY MEMBER_NO) ac
            ON ac.MEMBER_NO = M.MEMBER_NO
        WHERE M.MEMBER_NO = #{memberNo}
    </select>

    <!-- 닉네임만 조회 (채팅 알림용 경량 쿼리) -->
    <select id="selectNicknameByNo" resultType="string">
        SELECT NICKNAME FROM MEMBER WHERE MEMBER_NO = #{memberNo}
    </select>

    <!-- 이메일 중복 확인 -->
    <select id="checkEmailDuplicate" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE EMAIL = #{memberEmail}
          AND STATUS != 'W'
    </select>

    <!-- 닉네임 중복 확인 (memberNo 전달 시 본인 제외) -->
    <select id="checkNicknameDuplicate" resultType="int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE NICKNAME = #{memberNickname}
          AND STATUS != 'W'
          <if test="memberNo != null">
              AND MEMBER_NO != #{memberNo}
          </if>
    </select>

    <!-- 회원가입 -->
    <insert id="insertMember">
        INSERT INTO MEMBER (
            MEMBER_NO,
            EMAIL,
            PASSWORD,
            NICKNAME,
            NAME,
            PHONE,
            GENDER,
            AGE_RANGE,
            PROFILE_IMAGE,
            BIRTH_DATE,
            MEMBER_TYPE,
            STATUS,
            EMAIL_VERIFIED,
            LOGIN_TYPE
        ) VALUES (
            SEQ_MEMBER_NO.NEXTVAL,
            #{memberEmail},
            #{memberPw},
            #{memberNickname},
            #{memberName},
            #{memberPhone},
            #{memberGender},
            #{memberAgeGroup},
            #{memberProfileImg},
            TO_DATE(#{memberBirthDate}, 'YYYY-MM-DD'),
            'U',
            'A',
            'Y',
            #{loginType}
        )
    </insert>

    <!-- 회원 정보 수정 -->
    <update id="updateMember">
        UPDATE MEMBER
        SET
            NICKNAME = #{memberNickname},
            PHONE = #{memberPhone},
            PROFILE_IMAGE = #{memberProfileImg},
            INTRO = #{memberIntro},
            UPDATED_AT = SYSDATE
        WHERE MEMBER_NO = #{memberNo}
    </update>

    <!-- 회원 탈퇴 (상태 변경) -->
    <update id="withdrawMember">
        UPDATE MEMBER
        SET
            STATUS = 'W',
            UPDATED_AT = SYSDATE
        WHERE MEMBER_NO = #{memberNo}
    </update>

    <!-- 아이디 찾기 (이름 + 이메일) -->
    <select id="findId" resultMap="memberResultMap">
        SELECT
            EMAIL,
            CREATED_AT
        FROM MEMBER
        WHERE NAME = #{memberName}
          AND EMAIL = #{memberEmail}
          AND STATUS = 'A'
    </select>

    <!-- 비밀번호 찾기 (이메일 + 이름) -->
    <select id="findPw" resultMap="memberResultMap">
        SELECT
            MEMBER_NO,
            EMAIL,
            NAME
        FROM MEMBER
        WHERE EMAIL = #{memberEmail}
          AND NAME = #{memberName}
          AND STATUS = 'A'
    </select>

    <!-- 비밀번호 재설정 -->
    <update id="resetPw">
        UPDATE MEMBER
        SET
            PASSWORD = #{memberPw},
            UPDATED_AT = SYSDATE
        WHERE EMAIL = #{memberEmail}
    </update>

    <!-- 내가 작성한 자유게시판 글 (최근 10개) -->
    <select id="selectMyPosts" resultType="java.util.HashMap">
        SELECT * FROM (
            SELECT
                B.BOARD_NO AS "boardNo",
                B.TITLE AS "title",
                TO_CHAR(B.CREATED_AT, 'YYYY-MM-DD') AS "createdAt",
                (SELECT COUNT(*) FROM COMMENT_TBL C WHERE C.BOARD_NO = B.BOARD_NO AND C.STATUS = 'A') AS "commentCount",
                (SELECT COUNT(*) FROM BOARD_LIKE BL WHERE BL.BOARD_NO = B.BOARD_NO) AS "likeCount",
                ROW_NUMBER() OVER (ORDER BY B.CREATED_AT DESC) AS RN
            FROM BOARD B
            WHERE B.MEMBER_NO = #{memberNo}
              AND B.STATUS = 'A'
        )
        WHERE RN &lt;= 10
    </select>

    <!-- 내가 작성한 후기 (최근 10개) -->
    <select id="selectMyReviews" resultType="java.util.HashMap">
        SELECT * FROM (
            SELECT
                R.REVIEW_NO AS "reviewNo",
                R.TITLE AS "title",
                R.RATING AS "rating",
                TO_CHAR(R.CREATED_AT, 'YYYY-MM-DD') AS "createdAt",
                CB.TITLE AS "companionTitle",
                ROW_NUMBER() OVER (ORDER BY R.CREATED_AT DESC) AS RN
            FROM COMPANION_REVIEW R
            LEFT JOIN COMPANION_BOARD CB ON R.COMPANION_NO = CB.COMPANION_NO
            WHERE R.MEMBER_NO = #{memberNo}
              AND R.STATUS = 'Y'
        )
        WHERE RN &lt;= 10
    </select>

    <!-- 내가 좋아요한 글 (최근 10개) -->
    <select id="selectMyLikes" resultType="java.util.HashMap">
        SELECT * FROM (
            SELECT
                B.BOARD_NO AS "boardNo",
                B.TITLE AS "title",
                TO_CHAR(B.CREATED_AT, 'YYYY-MM-DD') AS "createdAt",
                M.NICKNAME AS "authorNickname",
                ROW_NUMBER() OVER (ORDER BY BL.CREATED_AT DESC) AS RN
            FROM BOARD_LIKE BL
            JOIN BOARD B ON BL.BOARD_NO = B.BOARD_NO
            JOIN MEMBER M ON B.MEMBER_NO = M.MEMBER_NO
            WHERE BL.MEMBER_NO = #{memberNo}
              AND B.STATUS = 'A'
        )
        WHERE RN &lt;= 10
    </select>

    <!-- 내가 작성한 동행 글 (최근 10개) -->
    <select id="selectMyCompanions" resultType="java.util.HashMap">
        SELECT * FROM (
            SELECT
                CB.COMPANION_NO AS "companionNo",
                CB.TITLE AS "title",
                CB.STATUS AS "status",
                CB.MAX_MEMBERS AS "maxMembers",
                TO_CHAR(CB.TRAVEL_DATE, 'YYYY-MM-DD') AS "travelDate",
                TO_CHAR(CB.CREATED_AT, 'YYYY-MM-DD') AS "createdAt",
                CB.IMAGE_URL AS "imageUrl",
                (SELECT COUNT(*) FROM COMPANION_JOIN J
                 WHERE J.COMPANION_NO = CB.COMPANION_NO
                   AND J.STATUS IN ('W', 'A')) AS "currentMembers",
                ROW_NUMBER() OVER (ORDER BY CB.CREATED_AT DESC) AS RN
            FROM COMPANION_BOARD CB
            WHERE CB.MEMBER_NO = #{memberNo}
              AND CB.STATUS != 'D'
        )
        WHERE RN &lt;= 10
    </select>

</mapper>
