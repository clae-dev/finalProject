# 개발노트 — 2026-02-28

> 포트폴리오 완성도 향상 작업 2일차
> 오늘의 방향: **올레길 콘텐츠 추가 + 기능 고도화 + 성능 최적화 + 날씨 예보 탭**

---

## 작업 목록

| 기능 | 파일 | 분류 |
|------|------|------|
| 제주 올레길 코스 조회 기능 추가 | `OlleTrails.jsx`, `OlleDetail.jsx` 외 | Feat |
| AI 창식이 대화 기록 localStorage 저장 | `AiChatPanel.jsx` | Feat |
| 동행 D-day 임박 색상 강조 | `Companions.jsx` | Feat |
| 날씨 위젯 3일 단기예보 탭 추가 | `WeatherSection.jsx` 외 | Feat |
| ReactQueryDevtools 프로덕션 번들 제거 | `App.jsx` | Perf |
| HeroSection LCP 이미지 우선순위 지정 | `HeroSection.jsx` | Perf |
| getDday 공용 유틸 추출 | `src/lib/companionUtils.js` (신규) | Refactor |
| CompanionsSection D-day 색상 누락 수정 | `CompanionsSection.jsx` | Fix |

---

## 상세 내역

---

### [Feat] 제주 올레길 코스 조회 기능 추가

**브랜치:** `1-feature-enhancement`
**배포:** EC2 (`http://52.78.65.4/olle`) 완료

---

#### 기획 배경

HONDI가 제주 혼자 여행 커뮤니티를 표방하는 만큼, 제주의 대표 도보 콘텐츠인 **올레길** 정보 조회 기능 추가. 백엔드 API 없이 프론트 정적 데이터로만 운영하는 방식을 선택했다.

**이유:**
- 올레길 26개 코스 데이터는 고정값 (공공 도메인) → DB 필요 없음
- 카카오맵 폴리라인 시각화 → 기존 `KakaoMap` 컴포넌트 확장으로 구현 가능
- 목록 필터 + 상세 지도 구조 → 포트폴리오에서 서비스 완결성 어필

---

#### 구현 파일

| 파일 | 유형 | 역할 |
|------|------|------|
| `src/data/olleTrails.js` | 신규 | 26개 코스 정적 데이터 (좌표 포함) |
| `src/pages/olle/OlleTrails.jsx` | 신규 | 코스 목록 페이지 (`/olle`) |
| `src/pages/olle/OlleDetail.jsx` | 신규 | 코스 상세 페이지 (`/olle/:courseNo`) |
| `src/components/common/KakaoMap.jsx` | 수정 | `path` prop 추가 → 폴리라인 렌더링 지원 |
| `src/App.jsx` | 수정 | `/olle`, `/olle/:courseNo` lazy 라우트 추가 |
| `src/components/common/Header.jsx` | 수정 | '여행' 드롭다운에 '올레길' 메뉴 항목 추가 |

---

#### 1. 정적 데이터 (`olleTrails.js`)

**코스 구성:** 메인 21코스 + 분기 5코스(1-1, 7-1, 10-1, 14-1, 18-1) = **총 26개**

```js
// 각 코스 객체 스키마
{
  id: Number,                   // 라우트 파라미터
  name: String,                 // "1코스", "1-1코스"
  subtitle: String,             // "시흥초등학교 → 광치기해변"
  region: "제주시" | "서귀포시",
  distance: Number,             // km
  difficulty: "하" | "중" | "상",
  duration: String,             // "4~5시간"
  terrain: String,              // "해안·오름"
  description: String,
  startPoint: { name, lat, lng },
  endPoint:   { name, lat, lng },
  stamps: String[],             // 스탬프 포인트 (4~5개)
  path: { lat, lng }[],         // 폴리라인 좌표 (5~9개)
}
```

**좌표 전략:**
공식 GPS 트랙은 포인트가 수백~수천 개라 그대로 쓰면 지도 렌더 성능에 부담. 코스당 **핵심 거점 5~9개**만 사용해 방향감은 살리되 세밀한 굴곡은 생략.

원형 코스(우도·가파도·추자도)는 `path` 배열의 첫 좌표 = 마지막 좌표로 설정해 폐합 경로 표현.

---

#### 2. KakaoMap 폴리라인 확장

기존 `KakaoMap.jsx`에 `path` prop 추가. 기존 마커 기능과 완전히 독립적으로 동작하므로 하위 호환 유지.

```js
// buildMap() 내부, setStatus('ready') 직전에 삽입
if (path && path.length > 1) {
  const linePath = path.map(p => new window.kakao.maps.LatLng(p.lat, p.lng));
  const polyline = new window.kakao.maps.Polyline({
    path: linePath,
    strokeWeight: 4,
    strokeColor: '#0ea5e9',  // sky-500
    strokeOpacity: 0.9,
    strokeStyle: 'solid',
  });
  polyline.setMap(map);
}
```

---

#### 3. OlleTrails 목록 페이지 (`/olle`)

- Hero 배너: Unsplash 제주 이미지 + 총 코스 수·총 거리 뱃지
- **sticky 필터 바**: 난이도(전체/하/중/상) × 지역(전체/제주시/서귀포시) 조합 필터
- **카드 그리드**: 3열(lg) → 2열(sm) → 1열(mobile) 반응형

```js
// useMemo로 필터 결과 메모이제이션
const filtered = useMemo(() => {
  return olleTrails.filter(t => {
    if (difficulty !== '전체' && t.difficulty !== difficulty) return false;
    if (region !== '전체' && t.region !== region) return false;
    return true;
  });
}, [difficulty, region]);
```

난이도 색상: 하=emerald / 중=amber / 상=red
카드 진입 시 `delay: i * 0.04` 스태거 애니메이션 적용.

---

#### 4. OlleDetail 상세 페이지 (`/olle/:courseNo`)

**지도 중심 자동 계산:**

```js
const center = useMemo(() => ({
  lat: trail.path.map(p => p.lat).reduce((a,b) => a+b) / trail.path.length,
  lng: trail.path.map(p => p.lng).reduce((a,b) => a+b) / trail.path.length,
}), [trail]);
```

**줌 레벨 자동 결정:**

```js
const mapLevel = useMemo(() => {
  const span = Math.max(latSpan, lngSpan);
  if (span > 0.3)  return 9;  // 추자도 등 원거리 섬
  if (span > 0.15) return 8;  // 장거리 (4코스 24.6km 등)
  if (span > 0.07) return 7;  // 중거리
  return 6;                   // 단거리
}, [trail]);
```

코스 길이가 10~25km로 제각각이라 고정 줌 레벨을 쓰면 일부 코스가 잘리거나 너무 확대되는 문제 → 좌표 범위(span) 기반 자동 계산으로 해결.

**순환 코스 마커 중복 방지:**

```js
// 출발 = 도착인 원형 코스는 도착 마커 생략
if (endPoint.lat !== startPoint.lat || endPoint.lng !== startPoint.lng) {
  ms.push({ ...endPoint, name: `■ 도착: ${endPoint.name}` });
}
```

**스탬프 타임라인:**
출발(emerald) → 경유(teal) → 도착(red) 색상 구분, 포인트 간 세로선으로 타임라인 시각화.

---

#### 5. 라우트 및 네비게이션

```js
// App.jsx — lazy 라우트 추가
const OlleTrails = lazy(() => import('./pages/olle/OlleTrails'));
const OlleDetail = lazy(() => import('./pages/olle/OlleDetail'));

<Route path="/olle"           element={<PageTransition><OlleTrails /></PageTransition>} />
<Route path="/olle/:courseNo" element={<PageTransition><OlleDetail /></PageTransition>} />
```

```js
// Header.jsx — '여행' 드롭다운에 추가
{ name: '올레길', path: '/olle', icon: <Footprints />, desc: '제주 올레 코스 안내' }
```

---

#### 6. 트레이드오프 및 한계

| 항목 | 현재 방식 | 개선 방향 |
|------|----------|-----------|
| 좌표 정확도 | 핵심 거점 5~9포인트 근사값 | GPX 공식 트랙 파일 사용 |
| 데이터 소스 | 수동 입력 정적 JSON | 올레길 공식 Open API 연동 |
| 코스 이미지 | Unsplash 공개 이미지 | 코스별 실제 사진 |
| 마커 색상 구분 | InfoWindow 텍스트(▶/■) | 커스텀 SVG 마커 이미지 |

---

### [Feat] AI 창식이 대화 기록 localStorage 저장

**파일:** `src/components/ai/AiChatPanel.jsx`

---

#### 기획 배경

AI 창식이와 대화를 나누다가 페이지를 새로고침하거나 다른 페이지로 이동했다가 돌아오면 대화 내용이 초기화되는 문제가 있었다. 챗봇 특성상 이전 맥락이 유지되는 것이 자연스러운 UX이고, 포트폴리오 관점에서도 "대화 지속성"은 완성도에 직결되는 요소라 개선을 결정.

---

#### 구현 내용

**변경 전:**

```js
const [messages, setMessages] = useState([WELCOME_MESSAGE]);
```

초기값이 항상 WELCOME_MESSAGE 하나짜리 배열 → 마운트마다 리셋.

**변경 후:**

```js
const STORAGE_KEY = 'changsik_chat_history';

function loadMessages() {
  try {
    const saved = localStorage.getItem(STORAGE_KEY);
    if (saved) {
      const parsed = JSON.parse(saved);
      if (Array.isArray(parsed) && parsed.length > 0) return parsed;
    }
  } catch {
    // 파싱 실패 시 무시
  }
  return [WELCOME_MESSAGE];
}

const [messages, setMessages] = useState(loadMessages);
```

`useState`에 초기값이 아닌 **초기화 함수(initializer function)**를 전달하는 패턴 사용.

> `useState(loadMessages)` vs `useState(loadMessages())`
> - 함수 참조를 넘기면 React가 **최초 마운트 시 한 번만** 호출
> - 괄호를 붙여 호출 결과를 넘기면 **렌더마다** 실행됨
> → 전자가 올바른 패턴. localStorage 접근 비용을 최초 마운트에만 지불.

---

**저장 useEffect 추가:**

```js
useEffect(() => {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(messages));
  } catch {
    // 저장 실패 시 무시 (용량 초과 등)
  }
}, [messages]);
```

`messages`가 바뀔 때마다 직렬화해서 저장. `try/catch`로 감싼 이유는 localStorage 용량 초과(`QuotaExceededError`) 시 앱이 크래시되지 않도록 방어하기 위함.

---

**초기화(↺) 버튼 연동:**

```js
const handleReset = () => {
  setMessages([WELCOME_MESSAGE]);
  setError(null);
  setInputValue('');
  localStorage.removeItem(STORAGE_KEY);  // 추가
};
```

state 리셋과 동시에 localStorage도 삭제. 삭제하지 않으면 다음 마운트 시 `loadMessages`가 웰컴 메시지 대신 이전 내용을 다시 복원해버리는 문제가 생김.

---

#### 동작 흐름

```
첫 방문
  └── loadMessages() → localStorage 없음 → [WELCOME_MESSAGE] 반환

대화 진행
  └── messages 변경 → useEffect → localStorage.setItem (자동 저장)

새로고침 / 재방문
  └── loadMessages() → localStorage 있음 → JSON.parse → 이전 대화 배열 반환
  └── 이전 대화 그대로 복원됨

초기화 버튼 클릭
  └── setMessages([WELCOME_MESSAGE]) + localStorage.removeItem
  └── 다음 마운트 시 다시 웰컴 메시지로 시작
```

---

#### 예외 처리

| 상황 | 처리 방식 |
|------|----------|
| localStorage 미지원 브라우저 | `try/catch` → 웰컴 메시지로 fallback |
| 저장 데이터 파싱 오류 (손상) | `try/catch` → 웰컴 메시지로 fallback |
| localStorage 용량 초과 | `try/catch` → 저장 실패 무시, 기존 대화는 유지 |
| 빈 배열 저장된 경우 | `Array.isArray && length > 0` 조건 → 웰컴 메시지로 fallback |

---

### [Feat] 동행 D-day 임박 색상 강조

**파일:** `src/pages/companion/Companions.jsx`

---

#### 기획 배경

`getDday()` 함수와 카드 이미지 위 D-day 배지는 이미 구현되어 있었으나, 모든 날짜가 동일한 흰 배경 + 하늘색 텍스트로 표시됐다. D-1과 D-30이 같은 색이면 긴박감 전달이 안 된다. 동행 모집의 특성상 마감 임박을 시각적으로 강조하면 참여 유도에도 효과적.

---

#### 구현 내용

`getDdayStyle(dday)` 헬퍼 함수를 추가하고, 배지 className을 동적으로 적용하는 방식으로 변경.

**변경 전:**

```jsx
{dday && (
  <span className="absolute top-4 right-4 px-3.5 py-1.5 bg-white/95 backdrop-blur-sm rounded-full text-xs font-bold text-sky-500 shadow-lg">
    {dday}
  </span>
)}
```

**변경 후:**

```js
function getDdayStyle(dday) {
  if (dday === 'D-Day') {
    return 'bg-orange-500 text-white shadow-orange-300/60 animate-pulse';
  }
  const n = parseInt(dday.replace('D-', ''), 10);
  if (n <= 3)  return 'bg-red-500 text-white shadow-red-300/60';
  if (n <= 7)  return 'bg-amber-400 text-white shadow-amber-300/60';
  return 'bg-white/95 text-sky-500 shadow-sky-100/60';
}
```

```jsx
{dday && (
  <span className={`absolute top-4 right-4 px-3.5 py-1.5 backdrop-blur-sm rounded-full text-xs font-bold shadow-lg ${getDdayStyle(dday)}`}>
    {dday}
  </span>
)}
```

---

#### 색상 기준

| D-day 값 | 배경색 | 의도 |
|-----------|--------|------|
| `D-Day` | 주황 (`orange-500`) + `animate-pulse` | 오늘이 여행 당일, 최우선 강조 |
| `D-1` ~ `D-3` | 빨강 (`red-500`) | 3일 이내 임박, 긴박감 |
| `D-4` ~ `D-7` | 노랑 (`amber-400`) | 1주일 내, 경고 수준 |
| `D-8` 이상 | 흰 배경 + 하늘색 (`sky-500`) | 여유 있음, 기존 스타일 유지 |

기준값 설정 근거:
- **D-3**: 비행기·숙소 예약 취소 위약금이 주로 3일 전부터 발생 → 사용자 입장에서 "결정해야 할" 데드라인
- **D-7**: 일반적으로 여행 준비(짐 챙기기 등)를 1주일 전부터 시작 → 서두를 필요가 있는 시점
- `animate-pulse`: Tailwind CSS 내장 애니메이션. 배지가 주기적으로 투명도 변화 → 당일 게시글이 시각적으로 튀어 보임

---

#### `getDday()` 기존 동작 (변경 없음)

```js
function getDday(travelDate) {
  // ISO (YYYY-MM-DD) 또는 M.D 형식 파싱
  // diff < 0 (지난 날짜) → null 반환 (배지 미표시)
  // diff = 0 → 'D-Day'
  // diff > 0 → 'D-{n}'
}
```

지난 날짜 게시글은 배지 자체가 표시되지 않으므로 `getDdayStyle`은 항상 양수 또는 'D-Day' 입력값만 받음. 음수 분기 처리 불필요.

---

### [Feat] 날씨 위젯 — 3일 단기예보 탭 추가

**관련 파일:**

| 파일 | 유형 |
|------|------|
| `backend/.../weather/service/WeatherService.java` | 수정 |
| `backend/.../weather/controller/WeatherController.java` | 수정 |
| `src/api/weather/weatherAPI.ts` | 수정 |
| `src/components/main/WeatherSection.jsx` | 수정 |

---

#### 기획 배경

날씨 위젯이 현재 시각 기온·날씨 상태만 보여주고 있어, "오늘 날씨는 알겠는데 내일·모레는?" 이라는 질문에 답을 못 했다. 기상청 API가 이미 연결돼 있으니, **단기예보 API(getVilageFcst)**를 추가 호출해 3일 예보를 탭으로 노출하는 방향으로 개선.

---

#### 백엔드 — WeatherService.java

기존에는 `getUltraSrtFcst`(초단기예보)만 호출하고 있었다. `getVilageFcst`(단기예보) 호출을 위한 `getForecast(city)` 메서드를 추가.

**base_time 계산:**

단기예보는 0200, 0500, 0800, 1100, 1400, 1700, 2000, 2300시에 발표되고, 발표 10분 후 조회 가능. 현재 시각 기준으로 가장 최근 발표 시각을 역순으로 탐색.

```java
private static final int[] FCST_BASE_HOURS = {2, 5, 8, 11, 14, 17, 20, 23};

// 발표 시각 + 10분 이상 지난 가장 최근 시각 선택
int curTotalMin = now.getHour() * 60 + now.getMinute();
for (int i = FCST_BASE_HOURS.length - 1; i >= 0; i--) {
    if (curTotalMin >= FCST_BASE_HOURS[i] * 60 + 10) {
        baseDateTime = now.withHour(FCST_BASE_HOURS[i])...;
        break;
    }
}
// 00시~02시10분 구간: 전날 23시 발표분 사용
```

**데이터 파싱 전략:**

단기예보는 시간별 카테고리가 12~14개 × 72시간 분량으로 응답. numOfRows=1000으로 한 번에 수신.

| 카테고리 | 처리 방식 |
|----------|----------|
| `TMN` (일 최저기온) | 날짜별 첫 출현값 사용 |
| `TMX` (일 최고기온) | 날짜별 첫 출현값 사용 |
| `TMP` (시간별 기온) | TMN/TMX 없을 때 min/max로 fallback |
| `SKY` (하늘상태) | 1200 → 1500 → 1300 → 첫값 우선순위 |
| `PTY` (강수형태) | 동일 |

낮 시간대(12시) 값을 대표값으로 쓰는 이유: 사용자 입장에서 "내일 날씨"를 판단할 때 낮 날씨가 가장 직관적이기 때문. 새벽 3시 흐림과 낮 12시 맑음이 같은 날이면, "맑음"으로 보여주는 게 자연스럽다.

```java
private int pickRepresentativeValue(Map<String, String> timeMap) {
    for (String t : new String[]{"1200", "1500", "1300", "1100", "0900"}) {
        if (timeMap.containsKey(t)) return parseInt(timeMap.get(t));
    }
    return parseInt(timeMap.values().iterator().next());
}
```

반환 구조: `List<Map>` — 오늘/내일/모레 3개 항목, 각 항목은 `date`, `label`, `sky`, `ptyCode`, `tmin`, `tmax` 포함.

---

#### 백엔드 — WeatherController.java

```java
@GetMapping("/api/weather/forecast")
public ResponseEntity<Map<String, Object>> getJejuForecast(
        @RequestParam(defaultValue = "jeju") String city) {
    List<Map<String, Object>> forecast = weatherService.getForecast(city);
    response.put("success", true);
    response.put("data", forecast);
    return ResponseEntity.ok(response);
}
```

---

#### 프론트엔드 — weatherAPI.ts

```ts
export interface ForecastDay {
  date: string;
  label: string;       // "오늘" | "내일" | "모레"
  sky: number;
  ptyCode: number;
  tmin: number;
  tmax: number;
}

export async function getJejuForecast(city = "jeju"): Promise<ApiResponse<ForecastDay[]>> {
  const response = await axiosApi.get("/api/weather/forecast", { params: { city } });
  return response.data;
}
```

---

#### 프론트엔드 — WeatherSection.jsx

**추가된 상태 및 쿼리:**

```js
const [mode, setMode] = useState('current'); // 'current' | 'forecast'

const { data: forecastData, isLoading: forecastLoading } = useQuery({
  queryKey: ['jejuForecast', city],
  queryFn: () => getJejuForecast(city),
  staleTime: 60 * 60 * 1000, // 1시간 (날씨 예보는 매시간 갱신)
  enabled: mode === 'forecast', // 예보 탭 진입 시 최초 1회만 호출
});
```

`enabled: mode === 'forecast'` 처리 이유: 사용자가 [예보] 탭을 누르기 전에는 API를 호출하지 않음. 위젯을 열었다고 자동으로 예보까지 불러오면 불필요한 API 요청이 발생한다.

**UI 구조 (탭 2단):**

```
[ 제주시 ][ 서귀포시]   ← 도시 탭 (기존)
[  지금  ][   예보  ]   ← 모드 탭 (신규)
─────────────────────
지금 모드: 습도 / 풍속 / 풍향 / 강수
예보 모드: 오늘 ⛅ 12° / 18°
           내일 🌧️ 10° / 15°
           모레 ☀️  8° / 16°
```

최저기온은 파란색(blue-400), 최고기온은 빨간색(red-400)으로 온도 의미를 색상으로도 전달.

---

## 성능 최적화

---

### 배경

기능 고도화 작업 이후, 코드 분석 중 발견한 성능·품질 이슈 4건을 연속으로 처리.

---

### [Perf] ReactQueryDevtools 프로덕션 번들 제거

**파일:** `src/App.jsx`

**문제:**

```jsx
// 변경 전 — 항상 렌더 → 프로덕션 빌드에도 devtools 번들 포함
<ReactQueryDevtools initialIsOpen={false} />
```

`@tanstack/react-query-devtools`는 개발용 패키지임에도 조건 없이 렌더되어 프로덕션 번들에 포함되고 있었다.

**수정:**

```jsx
// 변경 후 — DEV 환경에서만 렌더
{import.meta.env.DEV && <ReactQueryDevtools initialIsOpen={false} />}
```

Vite의 `import.meta.env.DEV`는 빌드 시점에 `false`로 정적 치환되므로, 트리쉐이킹에 의해 devtools 코드 전체가 프로덕션 번들에서 제거된다.

**빌드 결과 검증:**

```
# 수정 전: dist/assets/ 에 react-query-devtools 청크 존재
# 수정 후: devtools 청크 미생성 확인
✓ built in 904ms
```

---

### [Perf] HeroSection LCP 이미지 우선순위 지정

**파일:** `src/components/main/HeroSection.jsx`

**문제:**

히어로 슬라이드 이미지 3장이 전부 동일 속성으로 처리됨.

- 첫 번째 이미지: 페이지 로드 시 바로 보이는 **LCP(Largest Contentful Paint)** 대상 — 브라우저가 우선 다운로드해야 하지만 힌트가 없음
- 두·세 번째 이미지: 5초 후에야 표시되는데 즉시 다운로드됨 → 첫 번째 이미지 로딩을 방해

**수정:**

```jsx
// 변경 전
<img src={slide.image} alt="" className="..." />

// 변경 후
<img
  src={slide.image}
  alt=""
  className="..."
  loading={idx === 0 ? 'eager' : 'lazy'}
  fetchpriority={idx === 0 ? 'high' : 'low'}
/>
```

| 속성 | idx === 0 (첫 슬라이드) | idx > 0 (나머지) |
|------|------------------------|-----------------|
| `loading` | `eager` | `lazy` |
| `fetchpriority` | `high` | `low` |

- `fetchpriority="high"`: 브라우저 리소스 우선순위 힌트. 다른 이미지보다 먼저 요청.
- `loading="lazy"`: 두·세 번째 이미지는 뷰포트 진입 시점까지 다운로드 지연.

---

### [Refactor] getDday 공용 유틸 추출 + D-day 색상 누락 수정

**파일:** `src/lib/companionUtils.js` (신규), `Companions.jsx`, `CompanionsSection.jsx`

**문제 1 — 코드 중복:**

`getDday()`, `formatTravelDate()` 함수가 `Companions.jsx`와 `CompanionsSection.jsx` 두 곳에 **완전히 동일한 코드로 복사**되어 있었다.

**문제 2 — D-day 색상 누락:**

`Companions.jsx`(동행 목록)에 D-day 임박 색상 강조(`getDdayStyle`)를 추가했지만, `CompanionsSection.jsx`(메인 홈 동행 슬라이더)에는 적용이 누락돼 있었다. 홈에서는 여전히 흰 배경 단일 색상 배지가 표시되는 상태.

**해결:**

```
src/lib/companionUtils.js (신규)
├── getDday(travelDate)          — D-day 문자열 계산
├── getDdayStyle(dday)           — 임박도별 Tailwind 클래스 반환
└── formatTravelDate(travelDate) — 날짜 포맷 변환
```

두 파일 모두 로컬 함수를 제거하고 공용 유틸에서 import:

```js
import { getDday, getDdayStyle, formatTravelDate } from '../../lib/companionUtils';
```

`CompanionsSection.jsx`의 D-day 배지도 동일하게 `getDdayStyle(dday)` 적용 → 홈과 목록 페이지 동일한 시각 기준 유지.

**변경 규모:**

```
5 files changed, 53 insertions(+), 79 deletions(-)
```

79줄 삭제 — 중복 코드 완전 제거.

---

## 배포

| 단계 | 내용 |
|------|------|
| 빌드 | `npm run build` — 에러 없음 |
| EC2 초기화 | `rm -rf /home/ec2-user/dist/*` |
| SCP 업로드 | `dist/` → EC2 `/home/ec2-user/dist/` |
| Nginx reload | `sudo nginx -s reload` — OK |
| 확인 URL | `http://52.78.65.4` |

---

## 고도화 후보 목록 (미구현)

이번 세션에서 논의된 추가 고도화 아이디어:

| 항목 | 설명 | 난이도 |
|------|------|--------|
| 통합 검색 | 헤더 글로벌 검색 → 숙소/동행/리뷰 통합 결과 | 중 |
| 올레길 완주 기록 | 로그인 유저가 완주 코스 체크 → 마이페이지 연동 | 중 |
| 마이페이지 여행 발자국 지도 | 리뷰 작성 숙소·관광지를 카카오맵에 핀 시각화 | 상 |
| 동행 승인 시 자동 채팅방 생성 | 승인 후 채팅을 수동으로 개설하던 불편 개선 | 상 |
